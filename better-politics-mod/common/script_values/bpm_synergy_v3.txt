bpm_synergy_v3 = {
	#// Synergy disagreement categories
	#value = bpm_synergy_v3_denominator

	add = bpm_synergy_disagreement_governance_principle # // 0 to 20
	add = bpm_synergy_disagreement_democracy # // 0 to 20
	add = bpm_synergy_disagreement_economy # // 0 to 20
	if = {
		limit = {
			NOT = {
				has_law = law_type:law_slavery_banned
			}
		}
		add = bpm_synergy_disagreement_slavery
	}

	divide = bpm_synergy_v3_denominator
	max = 1
	min = 0
	#multiply = 100
}

bpm_synergy_v3_denominator = {
	value = 30
	if = {
		limit = {
			NOT = {
				has_law = law_type:law_slavery_banned
			}
		}
		add = 10
	}
}


bpm_synergy_disagreement_governance_principle = {
	value = 0

	if = {
		limit = {
			any_in_list = {
				variable = bpm_synergy_calculation_charset
				bpm_character_is_communist = yes
			}
			any_in_list = {
				variable = bpm_synergy_calculation_charset
				bpm_character_is_monarchist = yes
			}
		}
		value = 20
	}

	if = {
		limit = {
			any_in_list = {
				variable = bpm_synergy_calculation_charset
				bpm_character_is_republican = yes
			}
			any_in_list = {
				variable = bpm_synergy_calculation_charset
				bpm_character_is_monarchist = yes
			}
		}
		value = 10
	}


	if = {
		limit = {
			any_in_list = {
				variable = bpm_synergy_calculation_charset
				bpm_character_is_republican = yes
			}
			any_in_list = {
				variable = bpm_synergy_calculation_charset
				bpm_character_is_communist = yes
			}
		}
		value = 20
	}
}

bpm_synergy_disagreement_democracy = { 
	value = 0

	if = {
		limit = {
			any_in_list = {
				variable = bpm_synergy_calculation_charset
				bpm_character_is_radical_democrat = yes
			}
			any_in_list = {
				variable = bpm_synergy_calculation_charset
				bpm_character_is_moderate_democrat = yes
			}
		}
		value = 10
	}

	if = {
		limit = {
			any_in_list = {
				variable = bpm_synergy_calculation_charset
				bpm_character_is_radical_democrat = yes
			}
			any_in_list = {
				variable = bpm_synergy_calculation_charset
				bpm_character_is_authoritarian = yes
			}
		}
		value = 20
	}

	if = {
		limit = {
			any_in_list = {
				variable = bpm_synergy_calculation_charset
				bpm_character_is_moderate_democrat = yes
			}
			any_in_list = {
				variable = bpm_synergy_calculation_charset
				bpm_character_is_authoritarian = yes
			}
		}
		value = 15
	}
}

bpm_synergy_disagreement_slavery = {
	value = 0

	if = {
		limit = {
			any_in_list = {
				variable = bpm_synergy_calculation_charset
				bpm_character_is_abolitionist = yes
			}
			any_in_list = {
				variable = bpm_synergy_calculation_charset
				bpm_character_is_slaver = yes
			}
		}
		value = 10
	}
	if = {
		limit = {
			has_law = law_type:law_slavery_banned
		}
		value = 0
	}
}

bpm_synergy_disagreement_economy = {
	value = 0
	
	if = {
		limit = {
			any_in_list = {
				variable = bpm_synergy_calculation_charset
				bpm_character_is_capitalist_econ = yes
			}
			any_in_list = {
				variable = bpm_synergy_calculation_charset
				bpm_character_is_socialist_econ = yes
			}
		}
		value = 15
	}

	if = {
		limit = {
			any_in_list = {
				variable = bpm_synergy_calculation_charset
				bpm_character_is_capitalist_econ = yes
			}
			any_in_list = {
				variable = bpm_synergy_calculation_charset
				bpm_character_is_traditional_econ = yes
			}
		}
		value = 15
	}

	if = {
		limit = {
			any_in_list = {
				variable = bpm_synergy_calculation_charset
				bpm_character_is_socialist_econ = yes
			}
			any_in_list = {
				variable = bpm_synergy_calculation_charset
				bpm_character_is_traditional_econ = yes
			}
		}
		value = 20
	}

}