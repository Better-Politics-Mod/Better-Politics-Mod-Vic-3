bpm_cab_synergy_value_parl_base = {
    value = 200
    multiply = {
        value = 0
        every_interest_group = {
            limit = {
                OR = {
                    AND = {
                        is_in_government = yes
                        ig_approval >= unhappy
                    }
                    AND = {
                        is_in_government = no
                        ig_approval >= happy
                    }
                }
            }
            add = ig_clout
        }
    }
}

bpm_cab_sybergy_value_parl_base_gov = {
    value = 200
    multiply = {
        value = 0
        every_interest_group = {
            limit = {
                is_in_government = yes
                ig_approval >= unhappy
            }
            add = ig_clout
        }
    }
}

bpm_cab_sybergy_value_parl_base_gov_bloc = {
    value = 1
    multiply = {
        value = 0
        every_interest_group = {
            limit = {
                is_in_government = yes
                ig_approval >= unhappy
            }
            add = ig_clout
        }
    }
}

bpm_cab_synergy_value_parl_base_opposition = {
    value = 200
    multiply = {
        value = 0
        every_interest_group = {
            limit = {
                is_in_government = no
                ig_approval >= happy
            }
            add = ig_clout
        }
    }
}

bpm_cab_synergy_value_parl_base_opposition_bloc = {
    value = 1
    multiply = {
        value = 0
        every_interest_group = {
            limit = {
                is_in_government = no
                ig_approval >= happy
            }
            add = ig_clout
        }
    }
}

bpm_cab_synergy_value_stateexh = {
    value = 0
}

bpm_cab_synergy_value_stateexh_mult = {
    if = {
        limit = {
            OR = {
                bpm_country_has_parliamentary_electoral_system = yes
                bpm_country_has_provisional_government_system = yes
            }
        }
        value = 1
        divide = 50
    }
    else_if = {
        limit = {
            bpm_country_has_executive_electoral_system = yes
        }
        value = 1
        divide = 20
    }
    else_if = {
        limit = {
            OR = {
                bpm_country_has_executive_dictatorial_system = yes
                bpm_country_has_organic_electoral_system = yes
            }
        }
        value = 1
        divide = 10
    }
    else_if = {
        limit = {
            bpm_country_has_parliamentary_oligarchic_system = yes
        }
        value = 1
        divide = 20
    }
}

bpm_cab_synergy_value_hoshog_dis_mult = {
    if = {
        limit = {
            OR = {
                bpm_country_has_parliamentary_electoral_system = yes
                bpm_country_has_provisional_government_system = yes
            }
        }
        value = 20
    }
    else_if = {
        limit = {
            bpm_country_has_executive_electoral_system = yes
        }
        add = 0
    }
    else_if = {
        limit = {
            OR = {
                bpm_country_has_executive_dictatorial_system = yes
                bpm_country_has_organic_electoral_system = yes
            }
        }
        add = 0
    }
    else_if = {
        limit = {
            bpm_country_has_parliamentary_oligarchic_system = yes
        }
        add = 0
    }
}

bpm_cab_synergy_value_hoshog_dis = {
    add = {
        desc = "BPM_CABINET_SYNERGY_FROM_HOS_HOG_COMPATIBILITY"
        value = {
            value = bpm_cab_synergy_value_hoshog_dis_mult
            multiply = 0 # PLACEHOLDER
        }
    }
}


bpm_cab_synergy_value_parl = {
    value = 0
    add = {
        desc = "BPM_CABINET_SYNERGY_FROM_PARLIAMENTARY_APPROVAL_GOV"
        value = bpm_cab_sybergy_value_parl_base_gov
    }
    add = {
        desc = "BPM_CABINET_SYNERGY_FROM_PARLIAMENTARY_APPROVAL_OPPOSITION"
        value = bpm_cab_synergy_value_parl_base_opposition
    }
    subtract = {
        desc = "BPM_CABINET_SYNERGY_FROM_HOS_HOG_COMPATIBILITY"
        value = bpm_cab_synergy_value_hoshog_dis
    }
    subtract = {
        desc = "BPM_CABINET_SYNERGY_FROM_STATE_EXHAUSTION"
        value = bpm_cab_synergy_value_stateexh
    }
}

bpm_cab_synergy_value_pres = {
    value = 0
    add = {
        desc = "BPM_CABINET_SYNERGY_FROM_PRESIDENTIAL_BASE"
        value = 120
    }
    subtract = {
        desc = "BPM_CABINET_SYNERGY_FROM_HOS_HOG_COMPATIBILITY"
        value = bpm_cab_synergy_value_hoshog_dis
    }
    subtract = {
        desc = "BPM_CABINET_SYNERGY_FROM_STATE_EXHAUSTION"
        value = bpm_cab_synergy_value_stateexh
    }
}

bpm_cab_synergy_value_autocracy = {
    value = 0
    add = {
        desc = "BPM_CABINET_SYNERGY_FROM_AUTOCRATIC_BASE"
        value = 150
    }
    subtract = {
        desc = "BPM_CABINET_SYNERGY_FROM_HOS_HOG_COMPATIBILITY"
        value = bpm_cab_synergy_value_hoshog_dis
    }
    subtract = {
        desc = "BPM_CABINET_SYNERGY_FROM_STATE_EXHAUSTION"
        value = bpm_cab_synergy_value_stateexh
    }
}

bpm_cab_synergy_value_oligarchy = {
    value = 0
    add = {
        desc = "BPM_CABINET_SYNERGY_FROM_OLIGARCHIC_BASE"
        value = 150
    }
    subtract = {
        desc = "BPM_CABINET_SYNERGY_FROM_HOS_HOG_COMPATIBILITY"
        value = bpm_cab_synergy_value_hoshog_dis
    }
    subtract = {
        desc = "BPM_CABINET_SYNERGY_FROM_STATE_EXHAUSTION"
        value = bpm_cab_synergy_value_stateexh
    }
}

bpm_cab_synergy_value_high_abv = {
    value = owner.bpm_cab_synergy_value
    subtract = 80
    divide = 20
    min = 0
}

bpm_cab_synergy_value_mid_abv = {
    value = owner.bpm_cab_synergy_value
    subtract = 60
    divide = 20
    min = 0
}

bpm_cab_synergy_value_low_abv = {
    value = 60
    subtract = owner.bpm_cab_synergy_value
    divide = 60
}

bpm_cab_synergy_value = {
    value = 0
    if = {
        limit = {
            OR = {
                bpm_country_has_parliamentary_electoral_system = yes
                bpm_country_has_provisional_government_system = yes
            }
        }
        add = bpm_cab_synergy_value_parl
    }
    else_if = {
        limit = {
            bpm_country_has_executive_electoral_system = yes
        }
        add = bpm_cab_synergy_value_pres
    }
    else_if = {
        limit = {
            OR = {
                bpm_country_has_executive_dictatorial_system = yes
                bpm_country_has_organic_electoral_system = yes
            }
        }
        add = bpm_cab_synergy_value_autocracy
    }
    else_if = {
        limit = {
            bpm_country_has_parliamentary_oligarchic_system = yes
        }
        add = bpm_cab_synergy_value_oligarchy
    }

    add = modifier:country_legitimacy_base_add

    multiply = {
        value = 1
        add = modifier:country_synergy_mult
    }
    round = yes
    max = 100
    min = 0
} 

bpm_cab_char_synergy_value = {
    value = 0

    add = var:bpm_cab_char_synergy
}

bpm_synergy_with_executive = {
    save_temporary_scope_as = char
    value = 1
    subtract = { owner = {
        if = {
			limit = { modifier:country_bpm_hog_veto = yes }
			if = {
                limit = { bpm_has_acting_hog = yes }
                var:bpm_acting_hog ?= {
			    	save_temporary_scope_as = ochar
			    }
            }
            else = {
                var:bpm_headofgov ?= {
		        	save_temporary_scope_as = ochar
		        }
            }
		}
		else = {
			if = {
                limit = { bpm_has_gov_regency = yes }
			    var:bpm_regent ?= {
			    	save_temporary_scope_as = ochar
			    }
            }
            else = {
                ruler ?= {
                    save_temporary_scope_as = ochar
                }
            }
		}
        every_law = {
            limit = {
                save_temporary_scope_as = bpm_law_to_check
                NOT = { scope:ochar.bpm_character_law_stance_as_number = 0 }
                NOT = { scope:char.bpm_character_law_stance_as_number = 0 }
            }
            save_temporary_scope_as = bpm_law_to_check
            add = {
                value = scope:char.bpm_character_law_stance_as_number_abs
                subtract = scope:ochar.bpm_character_law_stance_as_number_abs
                if = {
                    limit = {
                        scope:char.bpm_character_law_stance_as_number_abs < scope:ochar.bpm_character_law_stance_as_number_abs
                    }
                    multiply = -1
                }
                divide = 4
            }
        }
        divide = bpm_synergy_of_pair_divisor
    } }
}

bpm_synergy_with_ruler = {
    save_temporary_scope_as = char
    value = 1
    subtract = { owner = {
        if = {
            limit = { bpm_has_gov_regency = yes }
		    var:bpm_regent ?= {
		    	save_temporary_scope_as = ochar
		    }
        }
        else = {
            ruler ?= {
                save_temporary_scope_as = ochar
            }
        }
        every_law = {
            limit = {
                save_temporary_scope_as = bpm_law_to_check
                NOT = { scope:ochar.bpm_character_law_stance_as_number = 0 }
                NOT = { scope:char.bpm_character_law_stance_as_number = 0 }
            }
            save_temporary_scope_as = bpm_law_to_check
            add = {
                value = scope:char.bpm_character_law_stance_as_number_abs
                subtract = scope:ochar.bpm_character_law_stance_as_number_abs
                if = {
                    limit = {
                        scope:char.bpm_character_law_stance_as_number_abs < scope:ochar.bpm_character_law_stance_as_number_abs
                    }
                    multiply = -1
                }
                divide = 4
            }
        }
        divide = bpm_synergy_of_pair_divisor
    } }
}

bpm_synergy_with_hog = {
    save_temporary_scope_as = char
    value = 1
    subtract = { owner = {
        if = {
            limit = { bpm_has_acting_hog = yes }
            var:bpm_acting_hog ?= {
		    	save_temporary_scope_as = ochar
		    }
        }
        else = {
            var:bpm_headofgov.leader ?= {
		    	save_temporary_scope_as = ochar
		    }
        }
        every_law = {
            limit = {
                save_temporary_scope_as = bpm_law_to_check
                NOT = { scope:ochar.bpm_character_law_stance_as_number = 0 }
                NOT = { scope:char.bpm_character_law_stance_as_number = 0 }
            }
            save_temporary_scope_as = bpm_law_to_check
            add = {
                value = scope:char.bpm_character_law_stance_as_number_abs
                subtract = scope:ochar.bpm_character_law_stance_as_number_abs
                if = {
                    limit = {
                        scope:char.bpm_character_law_stance_as_number_abs < scope:ochar.bpm_character_law_stance_as_number_abs
                    }
                    multiply = -1
                }
                divide = 4
            }
        }
        divide = bpm_synergy_of_pair_divisor
    } }
}

bpm_synergy_of_pair_divisor = {
    every_law = {
        limit = {
            save_temporary_scope_as = bpm_law_to_check
            NOT = { scope:ochar.bpm_character_law_stance_as_number = 0 }
            NOT = { scope:char.bpm_character_law_stance_as_number = 0 }
        }
        add = 1
    }
    min = 1
}

bpm_synergy_of_charset_divisor = {
    every_law = {
        add = 1
    }
    min = 1
}

bpm_absolute_opinion_raw = {
    value = scope:highest_opinion_char.bpm_character_law_stance_as_number_abs
    subtract = scope:lowest_opinion_char.bpm_character_law_stance_as_number_abs
}

bpm_character_law_stance_as_number_abs = {
    value = bpm_character_law_stance_as_number
    add = 2
}

bpm_character_law_stance_as_number_negative = {
    value = bpm_character_law_stance_as_number
    multiply = -1
}

bpm_character_law_stance_as_number = { 
    # we are in character scope
    # we have a law saved as scope:bpm_law_to_check
    value = 0
    if = {
        limit = {
            law_stance = {
                law = scope:bpm_law_to_check.type
                value = strongly_approve
            }
        }
        add = 2
    }
    else_if = {
        limit = {
            law_stance = {
                law = scope:bpm_law_to_check.type
                value = approve
            }
        }
        add = 1
    }
    else_if = {
        limit = {
            law_stance = {
                law = scope:bpm_law_to_check.type
                value = disapprove
            }
        }
        add = -1
    }
    else_if = {
        limit = {
            law_stance = {
                law = scope:bpm_law_to_check.type
                value = strongly_disapprove
            }
        }
        add = -2
    }
    else = {
        interest_group ?= {
            add = bpm_interest_group_law_stance_as_number
        }
    }
}

bpm_interest_group_law_stance_as_number = {
    if = {
        limit = {
            law_stance = {
                law = scope:bpm_law_to_check.type
                value = strongly_approve
            }
        }
        add = 2
    }
    else_if = {
        limit = {
            law_stance = {
                law = scope:bpm_law_to_check.type
                value = approve
            }
        }
        add = 1
    }
    else_if = {
        limit = {
            law_stance = {
                law = scope:bpm_law_to_check.type
                value = disapprove
            }
        }
        add = -1
    }
    else_if = {
        limit = {
            law_stance = {
                law = scope:bpm_law_to_check.type
                value = strongly_disapprove
            }
        }
        add = -2
    }
}

bpm_interest_group_law_enactment_stance_as_number = {
    scope:ig_to_check = {
        if = {
            limit = {
                law_enactment_stance = {
                    law = prev.type
                    value >= strongly_approve
                }
            }
            add = 2
        }
        else_if = {
            limit = {
                law_enactment_stance = {
                    law = prev.type
                    value = approve
                }
            }
            add = 1
        }
        else_if = {
            limit = {
                law_enactment_stance = {
                    law = prev.type
                    value = disapprove
                }
            }
            add = -1
        }
        else_if = {
            limit = {
                law_enactment_stance = {
                    law = prev.type
                    value <= strongly_disapprove
                }
            }
            add = -2
        }
    }
}

bpm_law_importance_value = {
    value = 1 

    if = {
        limit = {
            type = {
                is_same_law_group_as = law_type:law_serfdom
            }
            owner = { has_law = law_type:law_serfdom } 
        }
        add = 10
    }
    else_if = {
        limit = {
            type = {
                is_same_law_group_as = law_type:law_slave_trade
            }
            NOT = { owner = { has_law = law_type:law_slavery_banned } }
        }
        add = 10
    }

    if = {
        limit = {
            type = {
                is_same_law_group_as = law_type:law_monarchy
            }
        }
        add = 5
    }
    else_if = {
        limit = {
            type = {
                is_same_law_group_as = law_type:law_universal_suffrage
            }
        }
        add = 5
    }
}