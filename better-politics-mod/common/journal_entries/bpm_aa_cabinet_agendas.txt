je_bpm_cabinet_agendas = {
	icon = "gfx/interface/icons/event_icons/event_scales.dds"

	group = je_group_bpm_misc_mechanical

	status_desc = {
		first_valid = {
			triggered_desc = {
				desc = je_bpm_cabinet_agendas_desc
			}
		}
	}

	modifiers_while_active = {
		# add modifiers
	}

	is_shown_when_inactive = {
		# this should always be yes
		NOT = {
			is_country_type = decentralized
		}
		has_variable = bpm_has_cabinet
	}

	possible = {
		always = yes
		# starting triggers
	}

	immediate = {
		set_variable = {
			name = bpm_cabinet_agenda_je
			value = scope:journal_entry
		}
		scope:journal_entry = {
			set_variable = {
				name = bpm_cabinet_agenda_slot_one
				value = flag:bpm_cabinet_agenda_land_focus
			}
			set_variable = {
				name = bpm_cabinet_agenda_slot_one_progress
				value = 0.3
			}
			set_variable = {
				name = bpm_cabinet_agenda_slot_two
				value = flag:bpm_no_cabinet_agenda
			}
		}
	}

	invalid = {
		hidden_trigger = {
			always = no
		}
		# tooltip generation
	}

	on_weekly_pulse = {
		effect = {
			scope:journal_entry = {
				bpm_cabinet_agenda_weekly_progress = yes
				bpm_cabinet_agenda_completion = yes
				bpm_reset_cabinet_agendas_if_new_gov = yes
			}
			bpm_lock_slot_if_no_skill = yes
		}
	}

	on_monthly_pulse = {
		effect = {
			
		}
	}

	should_be_pinned_by_default = no
}

