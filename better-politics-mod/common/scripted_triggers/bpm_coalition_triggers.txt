bpm_can_revolt_ig = {
	NOT = {
		has_modifier = bpm_adapting_to_new_regime
	}
}

bpm_can_revolt_country = {
	NOT = {
		any_scope_state = {
			OR = {
				# possible for revcell to spawn
				AND = {
					bpm_revcell_recently_subsided = no
					turmoil >= 0.25
				}
				# already has revcell
				bpm_has_revcell = yes
			}
		}
	} 
}