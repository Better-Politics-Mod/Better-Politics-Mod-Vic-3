bpm_separation_of_powers = {
    value = 0

    if = {
        limit = {
            has_law = law_type:law_autocracy
        }
        add = {
            desc = "AUTOCRACY"
            value = 1
        }
    }
    if = {
        limit = {
            has_law = law_type:law_wealth_voting
        }
        add = {
            desc = "WEALTHVOTING"
            value = -1
        }
    }
}