bpm_update_hog = {
    every_scope_character = {
        limit = { has_role = bpm_character_role_hog }
        remove_character_role = bpm_character_role_hog
    }

    if = {
        limit = { 
            NOR = {
                AND = {
                    modifier:country_bpm_hog_selected_by_legislature = yes
                    modifier:country_bpm_ruler_complete_veto_enabled = yes
                }
                bpm_country_has_parliamentary_electoral_system = yes
            }
        }
        ordered_interest_group = {
            limit = {
                is_in_government = yes
            }
            position = 0
            order_by = ig_clout # PLACEHOLDER
            bpm_set_ig_as_hog = yes
        }
    }

    if = {
        limit = {
            ruler = var:bpm_headofgov.leader
        }
        ordered_scope_character = {
            limit = {
                interest_group ?= prev.var:bpm_headofgov
                is_character_alive = yes
                is_ruler_of_own_country = no
            }
            save_scope_as = selected_char
        }
        if = {
            limit = { NOT = { exists = scope:selected_char } }
            create_character = {
                interest_group = var:bpm_headofgov
                save_scope_as = selected_char
            }
        }
        scope:selected_char = {
            set_as_interest_group_leader = yes
        }
    }

    if = {
        limit = { modifier:country_bpm_unified_executive = no }
        var:bpm_headofgov.leader ?= {
            add_character_role = bpm_character_role_hog
            add_career_length = {role = bpm_character_role_hog years = 100}
        }
    }
}

bpm_set_ig_as_hog = {
    owner = {
        set_variable = {
            name = bpm_headofgov
            value = prev
        }
    }
}

bpm_make_character_historical_hog = {
    set_variable = bpm_historical_hog
    if = {
        limit = {
            NOT = { has_modifier = bpm_historical_hog }
        }
        add_modifier = {
            name = bpm_historical_hog
            days = normal_modifier_time
        }
    }
    if = {
        limit = { NOT = { exists = interest_group } }
        set_interest_group = owner.var:bpm_headofgov
    }
    if = {
        limit = {
            NOT = { interest_group.leader = this }
        }
        set_as_interest_group_leader = yes
    }
}

bpm_initialize_hog = {
    if = {
        limit = {
            any_scope_character = { has_modifier = bpm_historical_hog }
        }
        random_scope_character = {
            limit = { has_modifier = bpm_historical_hog }
            interest_group = {
                bpm_set_ig_as_hog = yes
            }
        }
    }
    else = {
        ordered_interest_group = {
            limit = {
                is_in_government = yes
            }
            alternative_limit = {
                always = yes
            }
            position = 0
            order_by = ig_clout
            bpm_set_ig_as_hog = yes
        }
    }
    bpm_update_hog = yes
}

bpm_update_regent = {
    if = {
        limit = {
            OR = {
                has_variable = bpm_regency_init
                AND = {
                    bpm_has_gov_regency = yes
                    OR = {
                        NOT = { exists = var:bpm_regent }
                        var:bpm_regent ?= {
                            is_character_alive = no
                        }
                    }
                }
            }
        }
        if = {
            limit = {
                any_scope_character = {
                    interest_group = {
                        is_in_government = yes
                    }
                    bpm_char_can_be_regent = yes
                }
            }
            ordered_interest_group = {
                limit = {
                        is_in_government = yes
                        any_scope_character = {
                            bpm_char_can_be_regent = yes
                        }
                    }
                order_by = ig_clout
                position = 0
                ordered_scope_character = {
                    limit = {
                        bpm_char_can_be_regent = yes
                    }
                    order_by = prominence
                    position = 0
                    bpm_set_character_as_regent = yes
                }
            }
        }
        else = {
            ordered_interest_group = {
                limit = {
                    is_in_government = yes
                }
                order_by = ig_clout
                position = 0
                save_temporary_scope_as = biggest_ig
            }
            create_character = {
	            age = 23
	            interest_group = scope:biggest_ig
	            on_created = {
	            	bpm_set_character_as_regent = yes
	            }
            }
        }
    }
}

bpm_remove_regency_modifier = {
    if = {
		limit = {
			has_modifier = modifier_regency_landowners
		}
		remove_modifier = modifier_regency_landowners
	}
	if = {
		limit = {
			has_modifier = modifier_regency_devout
		}
		remove_modifier = modifier_regency_devout
	}
	if = {
		limit = {
			has_modifier = modifier_regency_petty_bourgeoisie
		}
		remove_modifier = modifier_regency_petty_bourgeoisie
	}
	if = {
		limit = {
			has_modifier = modifier_regency_industrialists
		}
		remove_modifier = modifier_regency_industrialists
	}
	if = {
		limit = {
			has_modifier = modifier_regency_trade_unions
		}
		remove_modifier = modifier_regency_trade_unions
	}
	if = {
		limit = {
			has_modifier = modifier_regency_intelligentsia
		}
		remove_modifier = modifier_regency_intelligentsia
	}
	if = {
		limit = {
			has_modifier = modifier_regency_armed_forces
		}
		remove_modifier = modifier_regency_armed_forces
	}
	if = {
		limit = {
			has_modifier = modifier_regency_rural_folk
		}
		remove_modifier = modifier_regency_rural_folk
	}
	if = {
		limit = {
			has_modifier = modifier_regency_liberals
		}
		remove_modifier = modifier_regency_liberals
	}
	if = {
		limit = {
			has_modifier = modifier_regency_radicals
		}
		remove_modifier = modifier_regency_radicals
	}
	if = {
		limit = {
			has_modifier = modifier_regency_market_liberals
		}
		remove_modifier = modifier_regency_market_liberals
	}
	if = {
		limit = {
			has_modifier = modifier_regency_national_liberals
		}
		remove_modifier = modifier_regency_national_liberals
	}
	if = {
		limit = {
			has_modifier = modifier_regency_fascists
		}
		remove_modifier = modifier_regency_fascists
	}
	if = {
		limit = {
			has_modifier = modifier_regency_christian_democrats
		}
		remove_modifier = modifier_regency_christian_democrats
	}
	if = {
		limit = {
			has_modifier = modifier_regency_agrarian_populists
		}
		remove_modifier = modifier_regency_agrarian_populists
	}
	if = {
		limit = {
			has_modifier = modifier_regency_conservatives
		}
		remove_modifier = modifier_regency_conservatives
	}
	if = {
		limit = {
			has_modifier = modifier_regency_reactionaries
		}
		remove_modifier = modifier_regency_reactionaries
	}
	if = {
		limit = {
			has_modifier = modifier_regency_reformist_socialists
		}
		remove_modifier = modifier_regency_reformist_socialists
	}
	if = {
		limit = {
			has_modifier = modifier_regency
		}
		remove_modifier = modifier_regency
	}
}

bpm_end_regency = {
    bpm_remove_regency_modifier = yes
    every_scope_character = {
        limit = {has_role = bpm_character_role_regent}
        remove_character_role = bpm_character_role_regent
    }
    if = {
        limit = {has_variable = bpm_regent}
        remove_variable = bpm_regent
    }
    ruler ?= {
        set_variable = regency_complete
    }
}

bpm_set_character_as_regent = {
    save_temporary_scope_as = regent_scope
    owner = {
        every_scope_character = {
            limit = {has_role = bpm_character_role_regent}
            remove_character_role = bpm_character_role_regent
        }
        set_variable = {
            name = bpm_regent
            value = prev
        }
        bpm_remove_regency_modifier = yes
        add_regency_modifier = yes
    }
    add_character_role = bpm_character_role_regent
    add_career_length = {role = bpm_character_role_regent years = 100}
}

bpm_start_regency = {
    set_variable = bpm_regency_init
	bpm_update_regent = yes
	var:bpm_regent = {
		save_scope_as = regent_scope
	}
    remove_variable = bpm_regency_init
}

bpm_update_acting_hog = {
    if = {
        limit = {
            OR = {
                has_variable = bpm_acting_hog_init
                AND = {
                    bpm_has_acting_hog = yes
                    OR = {
                        NOT = { exists = var:bpm_acting_hog }
                        var:bpm_acting_hog ?= {
                            is_character_alive = no
                        }
                    }
                }
            }
        }
        if = {
            limit = {
                any_scope_character = {
                    interest_group = {
                        is_in_government = yes
                    }
                    bpm_char_can_be_acting_hog = yes
                }
            }
            ordered_interest_group = {
                limit = {
                        is_in_government = yes
                        any_scope_character = {
                            bpm_char_can_be_acting_hog = yes
                        }
                    }
                order_by = ig_clout
                position = 0
                ordered_scope_character = {
                    limit = {
                        bpm_char_can_be_acting_hog = yes
                    }
                    order_by = prominence
                    position = 0
                    bpm_set_character_as_acting_hog = yes
                }
            }
        }
        else = {
            ordered_interest_group = {
                limit = {
                    is_in_government = yes
                }
                order_by = ig_clout
                position = 0
                save_temporary_scope_as = biggest_ig
            }
            create_character = {
	            age = 23
	            interest_group = scope:biggest_ig
	            on_created = {
	            	bpm_set_character_as_acting_hog = yes
	            }
            }
        }
    }
}

bpm_set_character_as_acting_hog = {
    save_temporary_scope_as = acting_hog_scope
    owner = {
        every_scope_character = {
            limit = {has_role = bpm_character_role_acting_hog}
            remove_character_role = bpm_character_role_acting_hog
        }
        set_variable = {
            name = bpm_acting_hog
            value = prev
        }
        bpm_remove_acting_hog_modifier = yes
        add_acting_hog_modifier = yes
    }
    add_character_role = bpm_character_role_acting_hog
    add_career_length = {role = bpm_character_role_acting_hog years = 100}
}

bpm_start_acting_hog = {
    set_variable = bpm_acting_hog_init
	bpm_update_acting_hog = yes
	var:bpm_acting_hog = {
		save_scope_as = acting_hog_scope
	}
	remove_variable = bpm_acting_hog_init
}

bpm_remove_acting_hog_modifier = {
    if = {
		limit = {
			has_modifier = modifier_acting_hog_landowners
		}
		remove_modifier = modifier_acting_hog_landowners
	}
	if = {
		limit = {
			has_modifier = modifier_acting_hog_devout
		}
		remove_modifier = modifier_acting_hog_devout
	}
	if = {
		limit = {
			has_modifier = modifier_acting_hog_petty_bourgeoisie
		}
		remove_modifier = modifier_acting_hog_petty_bourgeoisie
	}
	if = {
		limit = {
			has_modifier = modifier_acting_hog_industrialists
		}
		remove_modifier = modifier_acting_hog_industrialists
	}
	if = {
		limit = {
			has_modifier = modifier_acting_hog_trade_unions
		}
		remove_modifier = modifier_acting_hog_trade_unions
	}
	if = {
		limit = {
			has_modifier = modifier_acting_hog_intelligentsia
		}
		remove_modifier = modifier_acting_hog_intelligentsia
	}
	if = {
		limit = {
			has_modifier = modifier_acting_hog_armed_forces
		}
		remove_modifier = modifier_acting_hog_armed_forces
	}
	if = {
		limit = {
			has_modifier = modifier_acting_hog_rural_folk
		}
		remove_modifier = modifier_acting_hog_rural_folk
	}
	if = {
		limit = {
			has_modifier = modifier_acting_hog_liberals
		}
		remove_modifier = modifier_acting_hog_liberals
	}
	if = {
		limit = {
			has_modifier = modifier_acting_hog_radicals
		}
		remove_modifier = modifier_acting_hog_radicals
	}
	if = {
		limit = {
			has_modifier = modifier_acting_hog_market_liberals
		}
		remove_modifier = modifier_acting_hog_market_liberals
	}
	if = {
		limit = {
			has_modifier = modifier_acting_hog_national_liberals
		}
		remove_modifier = modifier_acting_hog_national_liberals
	}
	if = {
		limit = {
			has_modifier = modifier_acting_hog_fascists
		}
		remove_modifier = modifier_acting_hog_fascists
	}
	if = {
		limit = {
			has_modifier = modifier_acting_hog_christian_democrats
		}
		remove_modifier = modifier_acting_hog_christian_democrats
	}
	if = {
		limit = {
			has_modifier = modifier_acting_hog_agrarian_populists
		}
		remove_modifier = modifier_acting_hog_agrarian_populists
	}
	if = {
		limit = {
			has_modifier = modifier_acting_hog_conservatives
		}
		remove_modifier = modifier_acting_hog_conservatives
	}
	if = {
		limit = {
			has_modifier = modifier_acting_hog_reactionaries
		}
		remove_modifier = modifier_acting_hog_reactionaries
	}
	if = {
		limit = {
			has_modifier = modifier_acting_hog_reformist_socialists
		}
		remove_modifier = modifier_acting_hog_reformist_socialists
	}
	if = {
		limit = {
			has_modifier = modifier_acting_hog
		}
		remove_modifier = modifier_acting_hog
	}
}

bpm_end_acting_hog = {
    bpm_remove_acting_hog_modifier = yes
    every_scope_character = {
        limit = {has_role = bpm_character_role_acting_hog}
        remove_character_role = bpm_character_role_acting_hog
    }
    if = {
        limit = {has_variable = bpm_acting_hog}
        remove_variable = bpm_acting_hog
    }
}