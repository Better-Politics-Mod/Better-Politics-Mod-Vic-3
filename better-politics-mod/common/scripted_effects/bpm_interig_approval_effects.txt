bpm_update_interig_approval = {
	every_activated_interest_group = {
		clear_variable_map = bpm_ig_salience_weights
		save_scope_as = ig
		owner = {
			every_law = {
				scope:ig = {
					add_to_variable_map = {
						name = bpm_ig_salience_weights
						key = prev.type
						value = prev.bpm_salience_weight
					}
				}
			}
		}
	}
}