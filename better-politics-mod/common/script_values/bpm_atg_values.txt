bpm_selected_government_interparty_hostility_value = {
	value = {
		value = var:bpm_selected_government_interparty_hostility
		multiply = -30
		round = yes
		desc = "bpm_selected_government_interparty_hostility_ideocoherence"
	}



	# 30+ Completely Hostile
	# These groups would try to kill each other with weapons in the streets
	# A government with this level of hostility would be completely dysfunctional
	# and may even cause a civil war

	# 20-29 Hostile
	# These groups would be at each other's throats
	# A government with this level of hostility would be very dysfunctional
	# They may function for very short periods of time, but would be very unstable

	# 10-19 Tense
	# These groups would be very wary of each other
	# A government with this level of hostility would be unstable

	# 1-9 Neutral
	# These groups would be indifferent or even friendly to each other
	# A government with this level of hostility would be stable

	# 0 United
	# These groups would be working together in harmony
	# A government with this can be considered as one single entity
	# There is no worry of a no-confidence vote
	if = {
		limit = {
			exists = py:reactionary_party
			is_target_in_variable_list = {
				name = bpm_in_gov_party_sel
				target = py:reactionary_party
			}
		}
		if = {
			limit = {
				exists = py:communist_party
				is_target_in_variable_list = {
					name = bpm_in_gov_party_sel
					target = py:communist_party
				}
			}
			add = {
				value = -30
				desc = "bpm_base_party_hostility"
			}
		}
		else_if = {
			limit = {
				exists = py:anarchist_party
				is_target_in_variable_list = {
					name = bpm_in_gov_party_sel
					target = py:anarchist_party
				}
			}
			add = {
				value = -30
				desc = "bpm_base_party_hostility"
			}
		}
		else_if = {
			limit = {
				exists = py:social_democrat_party
				is_target_in_variable_list = {
					name = bpm_in_gov_party_sel
					target = py:social_democrat_party
				}
			}
			add = {
				value = -20
				desc = "bpm_base_party_hostility"
			}
		}
		else_if = {
			limit = {
				exists = py:radical_party
				is_target_in_variable_list = {
					name = bpm_in_gov_party_sel
					target = py:radical_party
				}
			}
			add = {
				value = -20
				desc = "bpm_base_party_hostility"
			}
		}
		else_if = {
			limit = {
				exists = py:liberal_party
				is_target_in_variable_list = {
					name = bpm_in_gov_party_sel
					target = py:liberal_party
				}
			}
			add = {
				value = -10
				desc = "bpm_base_party_hostility"
			}
		}
	}
	else_if = {
		limit = {
			exists = py:reactionary_party
			is_target_in_variable_list = {
				name = bpm_in_gov_party_sel
				target = py:reactionary_party
			}
		}
		if = {
			limit = {
				exists = py:communist_party
				is_target_in_variable_list = {
					name = bpm_in_gov_party_sel
					target = py:communist_party
				}
			}
			add = {
				value = -30
				desc = "bpm_base_party_hostility"
			}
		}
		else_if = {
			limit = {
				exists = py:anarchist_party
				is_target_in_variable_list = {
					name = bpm_in_gov_party_sel
					target = py:anarchist_party
				}
			}
			add = {
				value = -30
				desc = "bpm_base_party_hostility"
			}
		}
		else_if = {
			limit = {
				exists = py:social_democrat_party
				is_target_in_variable_list = {
					name = bpm_in_gov_party_sel
					target = py:social_democrat_party
				}
			}
			add = {
				value = -10
				desc = "bpm_base_party_hostility"
			}
		}
		else_if = {
			limit = {
				exists = py:radical_party
				is_target_in_variable_list = {
					name = bpm_in_gov_party_sel
					target = py:radical_party
				}
			}
			add = {
				value = -10
				desc = "bpm_base_party_hostility"
			}
		}
	}

	if = {
		limit = {
			var:bpm_in_gov_party_sel_votes > 0.66
			variable_list_size = {
				name = bpm_in_gov_party_sel
				value >= 3
			}
		}
		add = {
			value = -10
			desc = "bpm_splintered_coalition"
		}
	}
	else_if = {
		limit = {
			var:bpm_in_gov_party_sel_votes > 0.75
			variable_list_size = {
				name = bpm_in_gov_party_sel
				value >= 3
			}
		}
		add = {
			value = -10
			desc = "bpm_splintered_coalition"
		}
	}
	else_if = {
		limit = {
			var:bpm_in_gov_party_sel_votes > 0.85
			variable_list_size = {
				name = bpm_in_gov_party_sel
				value >= 2
			}
		}
		add = {
			value = -10
			desc = "bpm_splintered_coalition"
		}
	}
	multiply = -1

	max = 30

}