# NB: When altering any triggers here, make sure to mirror those changes in japan_custom_loc to avoid trigger dissonance
REPLACE:tenpo_hardliner_goals_completed = {
	value = 0
	if = { # Tenpo - Hardliner: Social Control
		limit = {
			has_law_or_variant = law_type:law_secret_police
		}
		add = 1
	}
	if = { # Tenpo - Hardliner: Edo System: Strict
		limit = {
			has_law_or_variant = law_type:law_strict_edo_system
		}
		add = 1
	}
	if = { # Tenpo - Hardliner: Liberals Marginalized
		limit = {
            NOT = {
                any_interest_group = {
                    bpm_ig_is_liberal = yes
                    bpm_ig_is_marginal = no
                }
            }
		}
		add = 1
	}
	if = { # Tenpo - Hardliner: Rural
		limit = {
            country_has_building_type_levels = {
                target = bt:building_rice_farm
                value >= 40
            }
		}
		add = 1
	}
}

REPLACE:tenpo_reformer_goals_completed = {
	value = 0
	if = { # Tenpo - Reformer: Urbanization
		limit = {
            any_scope_state = {
                state_region = {
                    is_homeland = cu:japanese
                }
                any_scope_building = {
                    is_building_type = building_urban_center
                    level >= 3
                }
                count >= 3
            }
		}
		add = 1
	}
	if = { # Tenpo - Reformer: Edo System: Lax
		limit = {
			has_law_or_variant = law_type:law_lax_edo_system
		}
		add = 1
	}
	if = { # Tenpo - Reformer: Liberals in government
		limit = {
            any_interest_group = {
                bpm_ig_is_liberal = yes
                is_in_government = yes
            }
		}
		add = 1
	}
	if = { # Tenpo - Reformer: Bureaucrats
		limit = {
            NOT = {
                has_law_or_variant = law_type:law_hereditary_bureaucrats
            }
		}
		add = 1
	}
}

REPLACE:tenpo_total_agenda_goals_completed = {
	value = 0
	# Hardliner Agendas
	if = { # Tenpo - Hardliner: Social Control
		limit = {
			has_law_or_variant = law_type:law_secret_police
		}
		add = 1
	}
	if = { # Tenpo - Hardliner: Edo System: Strict
		limit = {
			has_law_or_variant = law_type:law_strict_edo_system
		}
		add = 1
	}
	if = { # Tenpo - Hardliner: Liberals Marginalized
		limit = {
            NOT = {
                any_interest_group = {
                    bpm_ig_is_liberal = yes
                    bpm_ig_is_marginal = no
                }
            }
		}
		add = 1
	}
	if = { # Tenpo - Hardliner: Rural
		limit = {
            country_has_building_type_levels = {
                target = bt:building_rice_farm
                value >= 40
            }
		}
		add = 1
	}
	# Reformer Agendas
	if = { # Tenpo - Reformer: Urbanization
		limit = {
            any_scope_state = {
                state_region = {
                    is_homeland = cu:japanese
                }
                any_scope_building = {
                    is_building_type = building_urban_center
                    level >= 3
                }
                count >= 3
            }
		}
		add = 1
	}
	if = { # Tenpo - Reformer: Edo System: Lax
		limit = {
			has_law_or_variant = law_type:law_lax_edo_system
		}
		add = 1
	}
	if = { # Tenpo - Reformer: Liberals in government
		limit = {
            any_interest_group = {
                bpm_ig_is_liberal = yes
                is_in_government = yes
            }
		}
		add = 1
	}
	if = { # Tenpo - Reformer: Bureaucrats
		limit = {
            NOT = {
                has_law_or_variant = law_type:law_hereditary_bureaucrats
            }
		}
		add = 1
	}
	# Shared Agendas
	if = { # Tenpo - Shared: Army Size
		limit = {
			army_power_projection > 500
		}
		add = 1
	}
	if = { # Tenpo - Shared: Naval
		limit = {
            country_has_building_type_levels = {
                target = bt:building_naval_fortification
                value >= 10
            }
		}
		add = 1
	}
	if = { # Tenpo - Shared: Famine
		limit = {
            NOT = {
                any_scope_state = {
                    state_region = {
                        is_homeland = cu:japanese
                    }
                    has_famine = yes
                }
            }
		}
		add = 1
	}
	if = { # Tenpo - Shared: GDP
		limit = {
			gdp >= var:tenpo_gdp_goal
		}
		add = 1
	}
}