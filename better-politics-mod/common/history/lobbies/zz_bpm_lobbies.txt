LOBBIES = {
	# Adding basic ideological IGs to starting lobbies
	every_country = {
		limit = {
			NOT = { this = c:GRE }
		}
		every_political_lobby = {
			if = { 
				limit = {
					any_lobby_member = { 
						OR = {
							is_interest_group_type = ig_landowners 
							is_interest_group_type = ig_devout
							is_interest_group_type = ig_armed_forces
						}
					}
					NOT = {
						any_lobby_member = {
							is_interest_group_type = ig_reactionaries
						}
					}
				}
				add_lobby_member = prev.ig:ig_reactionaries
			}
			if = { 
				limit = {
					any_lobby_member = { 
						OR = {
							is_interest_group_type = ig_landowners 
							is_interest_group_type = ig_devout
							is_interest_group_type = ig_armed_forces
						}
					}
					NOT = {
						any_lobby_member = {
							is_interest_group_type = ig_conservatives
						}
					}
				}
				add_lobby_member = prev.ig:ig_conservatives
			}
			if = { 
				limit = {
					any_lobby_member = { 
						OR = {
							is_interest_group_type = ig_intelligentsia
						}
					}
					NOT = {
						any_lobby_member = {
							is_interest_group_type = ig_liberals
						}
					}
				}
				add_lobby_member = prev.ig:ig_liberals
			}
		}
	}
}	







