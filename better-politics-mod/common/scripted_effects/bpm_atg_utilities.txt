bpm_update_government_and_opposition = {
	bpm_add_ig_to_their_desired_party = yes
	if = {
		limit = {
			OR = {
				bpm_country_has_executive_dictatorial_system = yes
				bpm_country_has_executive_electoral_system = yes
			}
		}
		bpm_form_government_exef_systems = yes
	}
	else_if = {
		limit = {
			bpm_country_has_organic_electoral_system = yes
		}
		bpm_form_government_organic_systems = yes
	}
	else_if = {
		limit = {
			bpm_country_has_parliamentary_oligarchic_system = yes
		}
		bpm_form_government_oligarchic_systems = yes
	}
	else_if = {
        limit = {
			bpm_country_has_provisional_government_system = yes
		}
		bpm_form_government_provgov_systems = yes
	}
	else = {
		bpm_form_government_parliamentary_electoral_systems = yes
	}
	every_interest_group = {
		limit = {
			bpm_ig_is_actively_revolting = yes
		}
		remove_ruling_interest_group = yes
	}
}

bpm_form_government_provgov_systems = {
	every_interest_group = {
		add_ruling_interest_group = yes
	}
	every_interest_group = {
		limit = {
			NOT = {
				has_ideology = ideology:ideology_provgov_member
			}
		}
		remove_ruling_interest_group = yes
	}
}

bpm_form_government_parliamentary_electoral_systems = {
	if = {
		limit = {
			has_variable_list = bpm_in_gov_party_sel
		}
		every_active_party = {
			every_member = {
				remove_ruling_interest_group = yes
			}
		}
		every_interest_group = {
			limit = {
				NOT = {
					exists = party
				}
				is_in_government = yes
			}
			remove_ruling_interest_group = yes
		}
		every_in_list = {
			variable = bpm_in_gov_party_sel
			every_member = {
				add_ruling_interest_group = yes
			}
		}
	}
	else = {
		bpm_form_government_exef_systems = yes
	}
}

bpm_add_ig_to_their_desired_party = {
	update_party_support = yes
	every_interest_group = {
		limit = {
			NOT = {
				exists = party
			}
			is_marginal = no
		}
		add_ruling_interest_group = yes
	}
	update_party_support = yes
	every_interest_group = {
		limit = {
			NOT = {
				exists = party
			}
			is_marginal = no
		}
		remove_ruling_interest_group = yes
	}
	update_party_support = yes
}

bpm_form_government_exef_systems = {
	every_interest_group = {
		add_ruling_interest_group = yes
	}
	ordered_interest_group = {
		limit = {
			ig_clout > 0
			trigger_if = {
				limit = {
					owner = {bpm_has_gov_regency = yes}
				}
				type = owner.var:bpm_regent.interest_group.type
			}
			trigger_else = {
				type = owner.ruler.interest_group.type
			}
		}
		alternative_limit = {
			always = yes
		}
		order_by = ig_clout
		position = 0
		check_range_bounds = no
		save_scope_as = ruler_ig
	}
	if = {
		limit = {
			any_active_party = {
				count > 0
			}
		}

		if = {
			limit = {
				exists = scope:ruler_ig.party
			}
			scope:ruler_ig.party = {
				set_ruling_party = yes
			}
			every_interest_group = {
				limit = {
					OR = {
						has_party = no
						NOT = {
							party = scope:ruler_ig.party
						}
					}
				}
				remove_ruling_interest_group = yes
			}
		}
		else = {
			every_interest_group = {
				limit = {
					NOR = {
						THIS = scope:ruler_ig.interest_group
					}
				}
				remove_ruling_interest_group = yes
			}
		}
	}
	else = {
		every_interest_group = {
			limit = {
				NOR = {
					THIS = scope:ruler_ig
					bpm_ig_is_state_institutional = yes
				}
			}
			remove_ruling_interest_group = yes
		}
	}

	update_party_support = yes
}

bpm_form_government_organic_systems = {
	every_interest_group = {
		add_ruling_interest_group = yes
	}

	every_interest_group = {
		limit = {
			NOT = {
				exists = party
			}
			is_in_government = yes
		}
		remove_ruling_interest_group = yes
	}

	every_interest_group = {
		limit = {
			has_party = yes
			party = {
				is_party_type = one_party_independents
			}
		}
		remove_ruling_interest_group = yes
	}

	update_party_support = yes
}

bpm_form_government_oligarchic_systems = {
	ordered_interest_group = {
		limit = {
			ig_clout > 0
			trigger_if = {
				limit = {
					owner = {bpm_has_gov_regency = yes}
				}
				type = owner.var:bpm_regent.interest_group.type
			}
			trigger_else = {
				type = owner.ruler.interest_group.type
			}
		}
		alternative_limit = {
			always = yes
		}
		order_by = ig_clout
		position = 0
		check_range_bounds = no
		save_scope_as = ruler_ig
	}
	every_interest_group = {
		add_ruling_interest_group = yes
	}
	every_interest_group = {
		limit = {
			NOR = {
				this = owner.var:bpm_mineco.interest_group
				this = owner.var:bpm_minint.interest_group
				this = owner.var:bpm_minsoc.interest_group
				this = owner.var:bpm_minmor.interest_group
				this = owner.var:bpm_minfor.interest_group
				bpm_ig_is_state_institutional = yes
				this = scope:ruler_ig
			}
		}
		remove_ruling_interest_group = yes
	}
}


bpm_calculate_selected_government_interparty_hostility = {
	clear_variable_list = bpm_synergy_calculation_charset

    every_in_list = {
		variable = bpm_in_gov_party_sel
		every_member = {
			leader = {
				owner = {
					add_to_variable_list = {
						name = bpm_synergy_calculation_charset
						target = prev
					}
				}
			}
		}
	}
	clear_variable_list = bpm_hostility_disagreements

    bpm_calculate_synergy = yes

    set_variable = {
        name = bpm_selected_government_interparty_hostility
        value = var:bpm_temp_synergy_calculation
    }

	if = {
		limit = {
			variable_list_size = {
				name = bpm_in_gov_party_sel
				value < 2
			}
		}
		set_variable = {
			name = bpm_selected_government_interparty_hostility
			value = 0 
		}
	}
	else = {
		bpm_disagreements_visualizing_helper = { tag = hostility }
	}
}

bpm_get_picked_pm_util = {
	save_scope_value_as = { name = counter value = 0 }
	every_in_list = {
		variable = bpm_potential_headofgov
		if = { 
			limit = { scope:counter = owner.var:bpm_choose_headofgov_carousel }
			save_scope_as = result 
		}
		save_scope_value_as = {
			name = counter
			value = {
				value = 1
				add = scope:counter
			}
		}
	}
}