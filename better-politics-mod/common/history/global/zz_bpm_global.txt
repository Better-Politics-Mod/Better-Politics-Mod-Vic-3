GLOBAL = {
	set_global_variable = bpm_2_4_save_var

	set_global_variable = {
		name = bpm_je_march_to_radicalism_progress
		value = 0
	}

	bpm_set_vanilla_ideologies_list = yes

	every_country = {	
		bpm_setup_country = yes
	}

	every_country = {
		limit = { 
			has_law = law_type:law_total_separation 
		}
		if = {
			limit = { 
				OR = {
					this ?= c:USA # Allegedly they were actually secular. Fixing state religion for them
				}
			}
		} else = {
			activate_law = law_type:law_freedom_of_conscience # Other countries will need to prove secularity to fit in.
		}
	}

	every_country = {
		bpm_setup_front_org = yes
		activate_law = law_type:law_no_labour_law
	}

	bpm_setup_global_laws_varlists = yes

	bpm_set_hidden_journal_entry_groups = yes
	bpm_setup_global_front_names = yes
	add_to_global_variable_list = {
		name = custom_button_list_flag
		target = flag:bpm_gui_sidebar_icon
	}
	add_to_global_variable_list = { name = bpm_sc_levels target = flag:lv1 }
	add_to_global_variable_list = { name = bpm_sc_levels target = flag:lv2 }
	add_to_global_variable_list = { name = bpm_sc_levels target = flag:lv3 }
	add_to_global_variable_list = { name = bpm_sc_levels target = flag:lv4 }

	every_country = {
		limit = {
			OR = {
				country_has_primary_culture = cu:south_german
				country_has_primary_culture = cu:north_german
				country_has_primary_culture = cu:alemannic
				this = c:BEL
			}
			religion_percent_country = { 
        		target = rel:catholic
                value >= 0.1 
            }
		}
		set_variable = local_christdem_split_var
		bpm_initialize_catholic_interests = yes
	}

	every_country = {
		bpm_update_consciousness = yes
		#bpm_setup_party_establishment = yes
    }    
}