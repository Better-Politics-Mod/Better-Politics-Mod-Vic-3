je_bpm_concession_military = {
	icon = "gfx/interface/icons/notification_icons/military.dds"

	group = bpm_je_group_concessions

	status_desc = {
		first_valid = {
			triggered_desc = {
				desc = je_bpm_concession_military_desc
			}
		}
	}

	modifiers_while_active = {
		bpm_military_concessions_modifier
	}
    
    immediate = {
        #set_variable = {
        #    name = bpm_cabinet_activate
        #    value = 0
        #}
    }

    complete = {
        scope:journal_entry = {
			is_goal_complete = yes
		}
		military_wage_level = very_high
	}

    on_complete = {
        #trigger_event = {
        #    id = bpm_cabinet_event.1
        #}
    }

    fail = {
    }

    on_fail = {
    }


    #is_shown_when_inactive = {
    #}

    #possible = {
    #}

    #on_weekly_pulse = {
    #    effect = {
    #    }
    #}

    current_value = {
        value = root.army_size
    }  

	timeout = 730

	on_timeout = {
		custom_tooltip = bpm_military_coup_happens
	}

	
    progressbar = yes

    goal_add_value = {
		add = root.army_size
        multiply = 0.5
    }


	should_be_pinned_by_default = yes
}
