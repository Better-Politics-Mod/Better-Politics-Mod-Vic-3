bpm_front_attraction_dispatch_overall = {
    if = {
        limit = {
            scope:selected_ig = { is_interest_group_type = ig_agrarian_populists }
        }
        value = bpm_front_attraction_dispatch_agrarian_populists
    }
    if = {
        limit = {
            scope:selected_ig = { is_interest_group_type = ig_anarchists }
        }
        value = bpm_front_attraction_dispatch_anarchists
    }
    if = {
        limit = {
            scope:selected_ig = { is_interest_group_type = ig_conservatives }
        }
        value = bpm_front_attraction_dispatch_conservatives
    }
    if = {
        limit = {
            scope:selected_ig = { is_interest_group_type = ig_fascists }
        }
        value = bpm_front_attraction_dispatch_fascists
    }
    if = {
        limit = {
            scope:selected_ig = { is_interest_group_type = ig_liberals }
        }
        value = bpm_front_attraction_dispatch_liberals
    }
    if = {
        limit = {
            scope:selected_ig = { is_interest_group_type = ig_market_liberals }
        }
        value = bpm_front_attraction_dispatch_market_liberals
    }
    if = {
        limit = {
            scope:selected_ig = { is_interest_group_type = ig_national_liberals }
        }
        value = bpm_front_attraction_dispatch_national_liberals
    }
    if = {
        limit = {
            scope:selected_ig = { is_interest_group_type = ig_radicals }
        }
        value = bpm_front_attraction_dispatch_radicals
    }
    if = {
        limit = {
            scope:selected_ig = { is_interest_group_type = ig_reactionaries }
        }
        value = bpm_front_attraction_dispatch_reactionaries
    }
    if = {
        limit = {
            scope:selected_ig = { is_interest_group_type = ig_reformist_socialists }
        }
        value = bpm_front_attraction_dispatch_reformist_socialists
    }
    if = {
        limit = {
            scope:selected_ig = { is_interest_group_type = ig_revolutionist_socialists }
        }
        value = bpm_front_attraction_dispatch_revolutionist_socialists
    }
    if = {
        limit = {
            scope:selected_ig = { is_interest_group_type = ig_socialists }
        }
        value = bpm_front_attraction_dispatch_socialists
    }
    if = {
        limit = {
            scope:selected_ig = { is_interest_group_type = ig_armed_forces }
        }
        value = bpm_front_attraction_dispatch_armed_forces
    }
    if = {
        limit = {
            scope:selected_ig = { is_interest_group_type = ig_devout }
        }
        value = bpm_front_attraction_dispatch_devout
    }
    if = {
        limit = {
            scope:selected_ig = { is_interest_group_type = ig_industrialists }
        }
        value = bpm_front_attraction_dispatch_industrialists
    }
    if = {
        limit = {
            scope:selected_ig = { is_interest_group_type = ig_intelligentsia }
        }
        value = bpm_front_attraction_dispatch_intelligentsia
    }
    if = {
        limit = {
            scope:selected_ig = { is_interest_group_type = ig_landowners }
        }
        value = bpm_front_attraction_dispatch_landowners
    }
    if = {
        limit = {
            scope:selected_ig = { is_interest_group_type = ig_petty_bourgeoisie }
        }
        value = bpm_front_attraction_dispatch_petty_bourgeoisie
    }
    if = {
        limit = {
            scope:selected_ig = { is_interest_group_type = ig_rural_folk }
        }
        value = bpm_front_attraction_dispatch_rural_folk
    }
    if = {
        limit = {
            scope:selected_ig = { is_interest_group_type = ig_trade_unions }
        }
        value = bpm_front_attraction_dispatch_trade_unions
    }
}