

namespace = bpm_no_confidence_nuked_event

bpm_no_confidence_nuked_event.1 = {
	type = country_event
	placement = root
	hidden = yes

	immediate = {
		every_active_party = {
			limit = {
				any_member = {
					is_in_government = yes
				}
			}
			add_momentum = -0.75
		}
	}
}