bpm_cabinet_agenda_slot_one_sgui = {
    scope = country
	# IS_ACTIVE
    is_shown = {
		je:je_bpm_cabinet_agendas = {
			# There is a slot
			has_variable = bpm_cabinet_agenda_slot_one
			# The slot is not empty
			NOT = {
				var:bpm_cabinet_agenda_slot_one = flag:bpm_no_cabinet_agenda
			}
		}
    }
	# IS_OPEN
    is_valid = {
		je:je_bpm_cabinet_agendas = {
			# There is a slot
			has_variable = bpm_cabinet_agenda_slot_one
			# The slot is empty
			var:bpm_cabinet_agenda_slot_one = flag:bpm_no_cabinet_agenda
		}
    }
}


bpm_cabinet_agenda_slot_two_sgui = {
    scope = country
	# IS_ACTIVE
    is_shown = {
		je:je_bpm_cabinet_agendas = {
			# There is a slot
			has_variable = bpm_cabinet_agenda_slot_two
			# The slot is not empty
			NOT = {
				var:bpm_cabinet_agenda_slot_two = flag:bpm_no_cabinet_agenda
			}
		}
    }
	# IS_OPEN
    is_valid = {
		je:je_bpm_cabinet_agendas = {
			# There is a slot
			has_variable = bpm_cabinet_agenda_slot_two
			# The slot is empty
			var:bpm_cabinet_agenda_slot_two = flag:bpm_no_cabinet_agenda
		}
    }
}



bpm_cabinet_agenda_slot_three_sgui = {
    scope = country
	# IS_ACTIVE
    is_shown = {
		je:je_bpm_cabinet_agendas = {
			# There is a slot
			has_variable = bpm_cabinet_agenda_slot_three
			# The slot is not empty
			NOT = {
				var:bpm_cabinet_agenda_slot_three = flag:bpm_no_cabinet_agenda
			}
		}
    }
	# IS_OPEN
    is_valid = {
		je:je_bpm_cabinet_agendas = {
			# There is a slot
			has_variable = bpm_cabinet_agenda_slot_three
			# The slot is empty
			var:bpm_cabinet_agenda_slot_three = flag:bpm_no_cabinet_agenda
		}
    }
}
