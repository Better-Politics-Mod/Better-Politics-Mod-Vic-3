je_bpm_modernizing_egypt = {
    # Not a graphics guy so chose something generic for now
    icon = "gfx/interface/icons/law_icons/jingoism.dds"
    group = je_group_historical_content

    modifiers_while_active = { bpm_modernizing_egypt }

    scripted_button = je_egypt_button_levant

    complete = {
        is_country_type = recognized
        has_law = law_type:law_public_schools
        has_law = law_type:law_professional_officers
    }

		fail = {
			NOT = {
				AND = {
					owns_entire_state_region = STATE_ALEPPO
					owns_entire_state_region = STATE_SYRIA
					owns_entire_state_region = STATE_TRANSJORDAN
					owns_entire_state_region = STATE_PALESTINE
					owns_entire_state_region = STATE_LEBANON
				}
			}
		}
    
    # Want to create a more fitting reward 
    on_complete = { activate_law = law_type:law_interventionism }

    # Forced opening of economy and repealing of state monopolies by European powers after Oriental Crisis
    on_fail = {
        activate_law = law_type:law_agrarianism
        activate_law = law_type:law_free_trade
    }

    weight = 100
    should_be_pinned_by_default = yes
}
