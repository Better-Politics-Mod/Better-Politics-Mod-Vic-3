GLOBAL = {
	every_country = {
		bpm_earlier_setup_country = yes
	}
	# workaround fix for countries losing their historical leaders on start
	every_country = {
		set_variable = {
			name = keep_current_ruler_var
			days = 7
		} 
	}
}
