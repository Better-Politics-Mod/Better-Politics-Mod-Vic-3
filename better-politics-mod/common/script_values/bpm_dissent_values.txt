bpm_ig_disapproval = {
	value = ig_approval
	multiply = -1
}

bpm_ig_revoluwant = {
	value = bpm_ig_disapproval
	multiply = var:bpm_ig_organization
	multiply = ig_clout
}

bpm_ig_organization_val = {
	value = var:bpm_ig_organization
	divide = 100
}

bpm_front_organization_change = {
	value = 0
	every_interest_group = {
		limit = {
			bpm_ig_in_front_check = yes
		}
		add = {
			value = var:bpm_ig_organization
			multiply = ig_clout
		}
	}
	every_political_movement = {
		limit = {
			bpm_movement_in_front_check = yes
		}
		add = {
			value = political_movement_radicalism
			multiply = political_movement_support
			multiply = 100
		}
	}
	divide = 10
}

# scopes to pass selected_front_name 
# and current_organization_value
bpm_resource_amount = {
	value = 0
	every_interest_group = {
		limit = {
			bpm_ig_in_front_check = yes
		}
		add = {
			add = ig_clout
		}
	}
	every_political_movement = {
		limit = {
			bpm_movement_in_front_check = yes
		}
		add = {
			add = political_movement_support
		}
	}
	multiply = scope:current_organization_value
	multiply = 50
}

bpm_resource_percentage = {
	value = var:bpm_revolution_resource_amount
	divide = var:bpm_revolution_resource_amount_max
}

bpm_revclockc = {
	value = var:revclockc
	modulo = 7
	divide = 5
}

bpm_will_change_nw = {
	if = {
		limit = {
			var:bpm_revolution_resource_amount > 400
			NAND = {
				var:bpm_left_alone_counter >= 2
				var:bpm_left_alone_counter <= 8
			}
		}
		value = -50
	}
	else_if = {
		limit = { bpm_left_alone_counter >= 2 }
		value = var:bpm_left_alone_counter
		multiply = 25
	} 
	else = {
		value = 0
	}
}


bpm_revolutionary_damage_normal = {
	value = modifier:country_bpm_revolutionary_damage_base_cost_add
	multiply = modifier:country_bpm_revolutionary_damage_efficiency_add
}

bpm_revolutionary_damage_double = {
	value = modifier:country_bpm_revolutionary_damage_base_cost_add
	multiply = modifier:country_bpm_revolutionary_damage_efficiency_add
	multiply = 5
}

bpm_revolutionary_cost_normal = {
	value = modifier:country_bpm_revolutionary_damage_base_cost_add
}

bpm_revolutionary_cost_double = {
	value = modifier:country_bpm_revolutionary_damage_base_cost_add
	multiply = 5
}

bpm_state_capacity = {
	value = modifier:country_authority_add
}

bpm_supporting_members_clout = {
	every_in_list = {
		variable = bpm_planning_subversive_action_ig
		add = ig_clout
	}
}

bpm_state_front_weight = {
	save_temporary_scope_as = selected_state
	owner = {
		every_interest_group = {
			limit = {
				bpm_ig_in_front_check = yes
			}
			save_temporary_scope_as = target_ig
			scope:selected_state = {
				add = {
					value = scope:target_ig.var:bpm_ig_organization
					multiply = bpm_pol_strength_share_ig
				}
			}
		}
	}
	multiply = 4
	min = 0
}

bpm_pol_strength_share_ig = {
    value = 0
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.0
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.01
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.02
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.03
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.04
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.05
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.06
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.07
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.08
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.09
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.1
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.11
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.12
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.13
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.14
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.15
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.16
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.17
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.18
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.19
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.2
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.21
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.22
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.23
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.24
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.25
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.26
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.27
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.28
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.29
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.3
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.31
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.32
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.33
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.34
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.35
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.36
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.37
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.38
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.39
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.4
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.41
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.42
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.43
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.44
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.45
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.46
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.47
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.48
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.49
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.5
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.51
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.52
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.53
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.54
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.55
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.56
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.57
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.58
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.59
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.6
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.61
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.62
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.63
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.64
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.65
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.66
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.67
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.68
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.69
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.7
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.71
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.72
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.73
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.74
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.75
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.76
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.77
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.78
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.79
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.8
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.81
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.82
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.83
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.84
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.85
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.86
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.87
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.88
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.89
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.9
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.91
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.92
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.93
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.94
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.95
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.96
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.97
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.98
            }
        }
        add = 0.01
    }
    if = {
        limit = {
            ig_state_pol_strength_share = {
                target = scope:target_ig
                value > 0.99
            }
        }
        add = 0.01
    }
}