
# agpop
bpm_front_attraction_dispatch_agrarian_populists = {
	scope:selected_ig = {

        if = {
			limit = {
				prev = flag:bpm_front_popular
			}
			value = bpm_front_attraction_u2
			multiply = bpm_front_popular_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_peasant
			}
			value = bpm_front_attraction_n3
			multiply = bpm_front_peasant_multiplier
		}

        else = {
            value = 0
        }
    }
}


# anarchists
bpm_front_attraction_dispatch_anarchists = {
	scope:selected_ig = {

        if = {
			limit = {
				prev = flag:bpm_front_labor
			}
			value = bpm_front_attraction_n1
			multiply = bpm_front_labor_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_popular
			}
			value = bpm_front_attraction_n2
			multiply = bpm_front_popular_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_peasant
			}
			value = bpm_front_attraction_n3
			multiply = bpm_front_peasant_multiplier
		}

        else = {
            value = 0
        }
    }
}


# mil
bpm_front_attraction_dispatch_armed_forces = {
	scope:selected_ig = {

        if = {
			limit = {
				prev = flag:bpm_front_bureaucratic
			}
			value = bpm_front_attraction_a3
			multiply = bpm_front_bureaucratic_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_reactionary
			}
			value = bpm_front_attraction_a1
			multiply = bpm_front_reactionary_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_fascist
			}
			value = bpm_front_attraction_a1
			multiply = bpm_front_fascist_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_nationalist
			}
			value = bpm_front_attraction_a1
			multiply = bpm_front_nationalist_multiplier
		}

        else = {
            value = 0
        }
    }
}


# bur
bpm_front_attraction_dispatch_petty_bourgeoisie = {
	scope:selected_ig = {

        if = {
			limit = {
				prev = flag:bpm_front_bureaucratic
			}
			value = bpm_front_attraction_a3
			multiply = bpm_front_bureaucratic_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_reactionary
			}
			value = bpm_front_attraction_a1
			multiply = bpm_front_reactionary_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_fascist
			}
			value = bpm_front_attraction_a1
			multiply = bpm_front_fascist_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_nationalist
			}
			value = bpm_front_attraction_a1
			multiply = bpm_front_nationalist_multiplier
		}

        else = {
            value = 0
        }
    }
}


# con
bpm_front_attraction_dispatch_conservatives = {
	scope:selected_ig = {

        if = {
			limit = {
				prev = flag:bpm_front_reactionary
			}
			value = bpm_front_attraction_a1
			multiply = bpm_front_reactionary_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_fascist
			}
			value = bpm_front_attraction_a1
			multiply = bpm_front_fascist_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_nationalist
			}
			value = bpm_front_attraction_a2
			multiply = bpm_front_nationalist_multiplier
		}

        else = {
            value = 0
        }
    }
}


# dev
bpm_front_attraction_dispatch_devout = {
	scope:selected_ig = {

        if = {
			limit = {
				prev = flag:bpm_front_bureaucratic
			}
			value = bpm_front_attraction_a3
			multiply = bpm_front_bureaucratic_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_reactionary
			}
			value = bpm_front_attraction_a1
			multiply = bpm_front_reactionary_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_fascist
			}
			value = bpm_front_attraction_a1
			multiply = bpm_front_fascist_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_nationalist
			}
			value = bpm_front_attraction_a1
			multiply = bpm_front_nationalist_multiplier
		}

        else = {
            value = 0
        }
    }
}


# fash
bpm_front_attraction_dispatch_fascists = {
	scope:selected_ig = {

        if = {
			limit = {
				prev = flag:bpm_front_reactionary
			}
			value = bpm_front_attraction_n2
			multiply = bpm_front_reactionary_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_fascist
			}
			value = bpm_front_attraction_n3
			multiply = bpm_front_fascist_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_nationalist
			}
			value = bpm_front_attraction_n2
			multiply = bpm_front_nationalist_multiplier
		}

        else = {
            value = 0
        }
    }
}


# busi
bpm_front_attraction_dispatch_industrialists = {
	scope:selected_ig = {

        if = {
			limit = {
				prev = flag:bpm_front_bureaucratic
			}
			value = bpm_front_attraction_a3
			multiply = bpm_front_bureaucratic_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_reactionary
			}
			value = bpm_front_attraction_a2
			multiply = bpm_front_reactionary_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_fascist
			}
			value = bpm_front_attraction_a2
			multiply = bpm_front_fascist_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_nationalist
			}
			value = bpm_front_attraction_a1
			multiply = bpm_front_nationalist_multiplier
		}

        else = {
            value = 0
        }
    }
}


# land
bpm_front_attraction_dispatch_landowners = {
	scope:selected_ig = {

        if = {
			limit = {
				prev = flag:bpm_front_bureaucratic
			}
			value = bpm_front_attraction_a3
			multiply = bpm_front_bureaucratic_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_reactionary
			}
			value = bpm_front_attraction_a2
			multiply = bpm_front_reactionary_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_fascist
			}
			value = bpm_front_attraction_a2
			multiply = bpm_front_fascist_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_nationalist
			}
			value = bpm_front_attraction_a1
			multiply = bpm_front_nationalist_multiplier
		}

        else = {
            value = 0
        }
    }
}


# lib
bpm_front_attraction_dispatch_liberals = {
	scope:selected_ig = {

        if = {
			limit = {
				prev = flag:bpm_front_liberal
			}
			value = bpm_front_attraction_a3
			multiply = bpm_front_liberal_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_republican
			}
			value = bpm_front_attraction_u2
			multiply = bpm_front_republican_multiplier
		}

        else = {
            value = 0
        }
    }
}


# mar
bpm_front_attraction_dispatch_market_liberals = {
	scope:selected_ig = {

        if = {
			limit = {
				prev = flag:bpm_front_liberal
			}
			value = bpm_front_attraction_a2
			multiply = bpm_front_liberal_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_republican
			}
			value = bpm_front_attraction_a2
			multiply = bpm_front_republican_multiplier
		}

        else = {
            value = 0
        }
    }
}


# nat
bpm_front_attraction_dispatch_national_liberals = {
	scope:selected_ig = {

        if = {
			limit = {
				prev = flag:bpm_front_liberal
			}
			value = bpm_front_attraction_a2
			multiply = bpm_front_liberal_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_republican
			}
			value = bpm_front_attraction_a1
			multiply = bpm_front_republican_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_reactionary
			}
			value = bpm_front_attraction_a1
			multiply = bpm_front_reactionary_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_fascist
			}
			value = bpm_front_attraction_a2
			multiply = bpm_front_fascist_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_nationalist
			}
			value = bpm_front_attraction_u2
			multiply = bpm_front_nationalist_multiplier
		}

        else = {
            value = 0
        }
    }
}


# prof
bpm_front_attraction_dispatch_intelligentsia = {
	scope:selected_ig = {

        if = {
			limit = {
				prev = flag:bpm_front_urban
			}
			value = bpm_front_attraction_a2
			multiply = bpm_front_urban_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_popular
			}
			value = bpm_front_attraction_a2
			multiply = bpm_front_popular_multiplier
		}

        else = {
            value = 0
        }
    }
}


# radicals DONE
bpm_front_attraction_dispatch_radicals = {
	scope:selected_ig = {

        if = {
			limit = {
				prev = flag:bpm_front_urban
			}
			value = bpm_front_attraction_u2
			multiply = bpm_front_urban_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_popular
			}
			value = bpm_front_attraction_u2
			multiply = bpm_front_popular_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_liberal
			}
			value = bpm_front_attraction_n2
			multiply = bpm_front_liberal_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_republican
			}
			value = bpm_front_attraction_n3
			multiply = bpm_front_republican_multiplier
		}

        else = {
            value = 0
        }
    }
}


# reac
bpm_front_attraction_dispatch_reactionaries = {
	scope:selected_ig = {

        if = {
			limit = {
				prev = flag:bpm_front_reactionary
			}
			value = bpm_front_attraction_u3
			multiply = bpm_front_reactionary_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_fascist
			}
			value = bpm_front_attraction_u2
			multiply = bpm_front_fascist_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_nationalist
			}
			value = bpm_front_attraction_u2
			multiply = bpm_front_nationalist_multiplier
		}

        else = {
            value = 0
        }
    }
}


# socdem
bpm_front_attraction_dispatch_reformist_socialists = {
	scope:selected_ig = {

        if = {
			limit = {
				prev = flag:bpm_front_labor
			}
			value = bpm_front_attraction_u2
			multiply = bpm_front_labor_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_urban
			}
			value = bpm_front_attraction_u2
			multiply = bpm_front_urban_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_socialist
			}
			value = bpm_front_attraction_u2
			multiply = bpm_front_socialist_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_popular
			}
			value = bpm_front_attraction_a2
			multiply = bpm_front_popular_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_republican
			}
			value = bpm_front_attraction_u1
			multiply = bpm_front_republican_multiplier
		}

        else = {
            value = 0
        }
    }
}


# revcom
bpm_front_attraction_dispatch_revolutionist_socialists = {
	scope:selected_ig = {

        if = {
			limit = {
				prev = flag:bpm_front_labor
			}
			value = bpm_front_attraction_n2
			multiply = bpm_front_labor_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_urban
			}
			value = bpm_front_attraction_n2
			multiply = bpm_front_urban_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_socialist
			}
			value = bpm_front_attraction_n2
			multiply = bpm_front_socialist_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_popular
			}
			value = bpm_front_attraction_n2
			multiply = bpm_front_popular_multiplier
		}

        else = {
            value = 0
        }
    }
}


# rurf
bpm_front_attraction_dispatch_rural_folks = {
	scope:selected_ig = {

        if = {
			limit = {
				prev = flag:bpm_front_reactionary
			}
			value = bpm_front_attraction_a1
			multiply = bpm_front_reactionary_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_peasant
			}
			value = bpm_front_attraction_u3
			multiply = bpm_front_peasant_multiplier
		}

        else = {
            value = 0
        }
    }
}


# soc
bpm_front_attraction_dispatch_socialists = {
	scope:selected_ig = {

        if = {
			limit = {
				prev = flag:bpm_front_labor
			}
			value = bpm_front_attraction_a2
			multiply = bpm_front_labor_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_urban
			}
			value = bpm_front_attraction_a2
			multiply = bpm_front_urban_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_socialist
			}
			value = bpm_front_attraction_a2
			multiply = bpm_front_socialist_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_popular
			}
			value = bpm_front_attraction_u2
			multiply = bpm_front_popular_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_republican
			}
			value = bpm_front_attraction_u1
			multiply = bpm_front_republican_multiplier
		}

        else = {
            value = 0
        }
    }
}


# tu
bpm_front_attraction_dispatch_trade_unions = {
	scope:selected_ig = {

        if = {
			limit = {
				prev = flag:bpm_front_labor
			}
			value = bpm_front_attraction_u3
			multiply = bpm_front_labor_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_urban
			}
			value = bpm_front_attraction_u2
			multiply = bpm_front_urban_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_socialist
			}
			value = bpm_front_attraction_u2
			multiply = bpm_front_socialist_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_popular
			}
			value = bpm_front_attraction_u2
			multiply = bpm_front_popular_multiplier
		}

        else = {
            value = 0
        }
    }
}


# labor DONE
bpm_front_attraction_dispatch_labor = {
	scope:selected_mov = {

        if = {
			limit = {
				prev = flag:bpm_front_labor
			}
			value = bpm_front_attraction_3
			multiply = bpm_front_labor_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_urban
			}
			value = bpm_front_attraction_2
			multiply = bpm_front_urban_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_socialist
			}
			value = bpm_front_attraction_2
			multiply = bpm_front_socialist_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_popular
			}
			value = bpm_front_attraction_2
			multiply = bpm_front_popular_multiplier
		}

        else = {
            value = 0
        }
    }
}


# socialist
bpm_front_attraction_dispatch_socialist = {
	scope:selected_mov = {

        if = {
			limit = {
				prev = flag:bpm_front_labor
			}
			value = bpm_front_attraction_2
			multiply = bpm_front_labor_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_urban
			}
			value = bpm_front_attraction_2
			multiply = bpm_front_urban_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_socialist
			}
			value = bpm_front_attraction_2
			multiply = bpm_front_socialist_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_popular
			}
			value = bpm_front_attraction_2
			multiply = bpm_front_popular_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_republican
			}
			value = bpm_front_attraction_1
			multiply = bpm_front_republican_multiplier
		}

        else = {
            value = 0
        }
    }
}


# anarchist
bpm_front_attraction_dispatch_anarchist = {
	scope:selected_mov = {

        if = {
			limit = {
				prev = flag:bpm_front_labor
			}
			value = bpm_front_attraction_1
			multiply = bpm_front_labor_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_popular
			}
			value = bpm_front_attraction_2
			multiply = bpm_front_popular_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_peasant
			}
			value = bpm_front_attraction_3
			multiply = bpm_front_peasant_multiplier
		}

        else = {
            value = 0
        }
    }
}


# communist
bpm_front_attraction_dispatch_communist = {
	scope:selected_mov = {

        if = {
			limit = {
				prev = flag:bpm_front_labor
			}
			value = bpm_front_attraction_2
			multiply = bpm_front_labor_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_urban
			}
			value = bpm_front_attraction_2
			multiply = bpm_front_urban_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_socialist
			}
			value = bpm_front_attraction_2
			multiply = bpm_front_socialist_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_popular
			}
			value = bpm_front_attraction_2
			multiply = bpm_front_popular_multiplier
		}

        else = {
            value = 0
        }
    }
}


# reformist
bpm_front_attraction_dispatch_reformist_socialist = {
	scope:selected_mov = {

        if = {
			limit = {
				prev = flag:bpm_front_labor
			}
			value = bpm_front_attraction_2
			multiply = bpm_front_labor_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_urban
			}
			value = bpm_front_attraction_2
			multiply = bpm_front_urban_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_socialist
			}
			value = bpm_front_attraction_2
			multiply = bpm_front_socialist_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_popular
			}
			value = bpm_front_attraction_2
			multiply = bpm_front_popular_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_republican
			}
			value = bpm_front_attraction_1
			multiply = bpm_front_republican_multiplier
		}

        else = {
            value = 0
        }
    }
}


# liberal
bpm_front_attraction_dispatch_liberal = {
	scope:selected_mov = {

        if = {
			limit = {
				prev = flag:bpm_front_liberal
			}
			value = bpm_front_attraction_3
			multiply = bpm_front_liberal_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_republican
			}
			value = bpm_front_attraction_2
			multiply = bpm_front_republican_multiplier
		}

        else = {
            value = 0
        }
    }
}


# marlib
bpm_front_attraction_dispatch_market_liberal = {
	scope:selected_mov = {

        if = {
			limit = {
				prev = flag:bpm_front_liberal
			}
			value = bpm_front_attraction_2
			multiply = bpm_front_liberal_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_republican
			}
			value = bpm_front_attraction_2
			multiply = bpm_front_republican_multiplier
		}

        else = {
            value = 0
        }
    }
}


# natlib
bpm_front_attraction_dispatch_national_liberal = {
	scope:selected_mov = {

        if = {
			limit = {
				prev = flag:bpm_front_liberal
			}
			value = bpm_front_attraction_2
			multiply = bpm_front_liberal_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_republican
			}
			value = bpm_front_attraction_2
			multiply = bpm_front_republican_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_reactionary
			}
			value = bpm_front_attraction_1
			multiply = bpm_front_reactionary_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_fascist
			}
			value = bpm_front_attraction_2
			multiply = bpm_front_fascist_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_nationalist
			}
			value = bpm_front_attraction_2
			multiply = bpm_front_nationalist_multiplier
		}

        else = {
            value = 0
        }
    }
}


# modernizer
bpm_front_attraction_dispatch_modernizer = {
	scope:selected_mov = {

        if = {
			limit = {
				prev = flag:bpm_front_liberal
			}
			value = bpm_front_attraction_3
			multiply = bpm_front_liberal_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_republican
			}
			value = bpm_front_attraction_2
			multiply = bpm_front_republican_multiplier
		}

        else = {
            value = 0
        }
    }
}


# radical
bpm_front_attraction_dispatch_radical = {
	scope:selected_mov = {

        if = {
			limit = {
				prev = flag:bpm_front_urban
			}
			value = bpm_front_attraction_2
			multiply = bpm_front_urban_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_popular
			}
			value = bpm_front_attraction_2
			multiply = bpm_front_popular_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_liberal
			}
			value = bpm_front_attraction_2
			multiply = bpm_front_liberal_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_republican
			}
			value = bpm_front_attraction_3
			multiply = bpm_front_republican_multiplier
		}

        else = {
            value = 0
        }
    }
}


# reactionary
bpm_front_attraction_dispatch_reactionary = {
	scope:selected_mov = {

        if = {
			limit = {
				prev = flag:bpm_front_reactionary
			}
			value = bpm_front_attraction_3
			multiply = bpm_front_reactionary_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_fascist
			}
			value = bpm_front_attraction_2
			multiply = bpm_front_fascist_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_nationalist
			}
			value = bpm_front_attraction_2
			multiply = bpm_front_nationalist_multiplier
		}

        else = {
            value = 0
        }
    }
}


# nationalist
bpm_front_attraction_dispatch_cultural_majority = {
	scope:selected_mov = {

        if = {
			limit = {
				prev = flag:bpm_front_reactionary
			}
			value = bpm_front_attraction_2
			multiply = bpm_front_reactionary_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_fascist
			}
			value = bpm_front_attraction_2
			multiply = bpm_front_fascist_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_nationalist
			}
			value = bpm_front_attraction_3
			multiply = bpm_front_nationalist_multiplier
		}

        else = {
            value = 0
        }
    }
}


# absolutist
bpm_front_attraction_dispatch_royalist_absolutist = {
	scope:selected_mov = {

        if = {
			limit = {
				prev = flag:bpm_front_reactionary
			}
			value = bpm_front_attraction_3
			multiply = bpm_front_reactionary_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_fascist
			}
			value = bpm_front_attraction_2
			multiply = bpm_front_fascist_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_nationalist
			}
			value = bpm_front_attraction_2
			multiply = bpm_front_nationalist_multiplier
		}

        else = {
            value = 0
        }
    }
}


# constitutionalist
bpm_front_attraction_dispatch_royalist_constitutional = {
	scope:selected_mov = {

        if = {
			limit = {
				prev = flag:bpm_front_liberal
			}
			value = bpm_front_attraction_3
			multiply = bpm_front_liberal_multiplier
		}

        else = {
            value = 0
        }
    }
}


# fascist
bpm_front_attraction_dispatch_fascist = {
	scope:selected_mov = {

        if = {
			limit = {
				prev = flag:bpm_front_reactionary
			}
			value = bpm_front_attraction_2
			multiply = bpm_front_reactionary_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_fascist
			}
			value = bpm_front_attraction_3
			multiply = bpm_front_fascist_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_nationalist
			}
			value = bpm_front_attraction_2
			multiply = bpm_front_nationalist_multiplier
		}

        else = {
            value = 0
        }
    }
}


# peasant
bpm_front_attraction_dispatch_land_reform = {
	scope:selected_mov = {

        if = {
			limit = {
				prev = flag:bpm_front_reactionary
			}
			value = bpm_front_attraction_1
			multiply = bpm_front_reactionary_multiplier
		}

        else_if = {
			limit = {
				prev = flag:bpm_front_peasant
			}
			value = bpm_front_attraction_3
			multiply = bpm_front_peasant_multiplier
		}

        else = {
            value = 0
        }
    }
}
