namespace = bpm_issues_of_elections

# Attack on issue
bpm_issues_of_elections.1 = {
	type = country_event
	placement = root
	title = bpm_issues_of_elections.1.t
	desc = bpm_issues_of_elections.1.d
	flavor = bpm_issues_of_elections.1.f

	dlc = dlc_bpm

	duration = 3

	gui_window = event_window_highlander


    icon = "gfx/interface/icons/event_icons/event_election.dds"

	event_image = {
		video = "southamerica_election"
	}

	left_icon = scope:first_party_leader
	right_icon = scope:second_party_leader

	on_created_soundeffect = "event:/SFX/UI/Alerts/event_appear"

	trigger = {
		any_in_list = {
			variable = bpm_country_issues
		}
	}

	immediate = {
		random_active_party = {
			save_scope_as = first_party
			ordered_member = {
				order_by = ig_clout
				position = 0
				leader = {
					save_scope_as = first_party_leader
				}
			}
		}

		remove_variable = bpm_current_target_issue

		scope:first_party_leader.interest_group = {
			var:bpm_party_issue = {
				save_scope_as = bpm_selected_issue
			}
		}

		set_variable = {
			name = bpm_current_target_issue
			value = scope:bpm_selected_issue
		}
		
		random_active_party = {
			limit = {
				NOT = {
					any_member = {
						this = scope:first_party_leader.interest_group
					}
				}
			}
			save_scope_as = second_party
			ordered_member = {
				order_by = ig_clout
				position = 0
				leader = {
					save_scope_as = second_party_leader
				}
			}
		}
	}

	option = {
		name = bpm_issues_of_elections.1.a
		custom_tooltip = bpm_issue_relevance_up_tt
		hidden_effect = {
			save_scope_value_as = {
				name = macro_value
				value = 0.05
			}
			bpm_add_macro_issue = yes
		}
		scope:first_party = {
			add_momentum = 0.2
		}
		scope:second_party = {
			add_momentum = -0.1
		}
	}

	option = {
		name = bpm_issues_of_elections.1.b
		custom_tooltip = bpm_issue_relevance_very_up_tt
		hidden_effect = {
			save_scope_value_as = {
				name = macro_value
				value = 0.1
			}
			bpm_add_macro_issue = yes
		}
		custom_tooltip = bpm_issues_of_elections.1.b_tt
		scope:second_party = {
			add_momentum = -0.3
		}
	}
}

# Debug
bpm_issues_of_elections.2 = {
	type = country_event
	placement = root
	hidden = yes

	immediate = {
		bpm_setup_party_issues_campaign = yes
	}
}

# Rally up on issue
bpm_issues_of_elections.3 = {
	type = country_event
	placement = root
	title = bpm_issues_of_elections.3.t
	desc = bpm_issues_of_elections.3.d
	flavor = bpm_issues_of_elections.3.f

	dlc = dlc_bpm

	duration = 3

	gui_window = event_window_1char_adventure


    icon = "gfx/interface/icons/event_icons/event_election.dds"

	event_image = {
		video = "southamerica_election"
	}

	left_icon = scope:first_party_leader
	#right_icon = scope:second_party_leader

	on_created_soundeffect = "event:/SFX/UI/Alerts/event_appear"

	trigger = {
		any_in_list = {
			variable = bpm_country_issues
		}
	}

	immediate = {
		random_active_party = {
			save_scope_as = first_party
			ordered_member = {
				order_by = ig_clout
				position = 0
				leader = {
					save_scope_as = first_party_leader
				}
			}
		}

		remove_variable = bpm_current_target_issue

		scope:first_party_leader.interest_group = {
			var:bpm_party_issue = {
				save_scope_as = bpm_selected_issue
			}
		}

		set_variable = {
			name = bpm_current_target_issue
			value = scope:bpm_selected_issue
		}
	}

	option = {
		name = bpm_issues_of_elections.3.a
		custom_tooltip = bpm_issue_relevance_up_tt
		hidden_effect = {
			save_scope_value_as = {
				name = macro_value
				value = 0.1
			}
			bpm_add_macro_issue = yes
		}
		scope:first_party = {
			add_momentum = 0.1
		}
	}

	option = {
		name = bpm_issues_of_elections.3.b
		custom_tooltip = bpm_issue_relevance_very_up_tt
		hidden_effect = {
			save_scope_value_as = {
				name = macro_value
				value = 0.3
			}
			bpm_add_macro_issue = yes
		}
		scope:first_party = {
			add_momentum = -0.3
		}
	}
}


# Attack on issue
bpm_issues_of_elections.4 = {
	type = country_event
	placement = root
	title = bpm_issues_of_elections.4.t
	desc = bpm_issues_of_elections.4.d
	flavor = bpm_issues_of_elections.4.f

	dlc = dlc_bpm

	duration = 3

	gui_window = event_window_highlander


    icon = "gfx/interface/icons/event_icons/event_election.dds"

	event_image = {
		video = "southamerica_election"
	}

	left_icon = scope:second_party_leader
	right_icon = scope:first_party_leader

	on_created_soundeffect = "event:/SFX/UI/Alerts/event_appear"

	trigger = {
		any_in_list = {
			variable = bpm_country_issues
		}
	}

	immediate = {
		random_active_party = {
			save_scope_as = first_party
			ordered_member = {
				order_by = ig_clout
				position = 0
				leader = {
					save_scope_as = first_party_leader
				}
			}
		}

		remove_variable = bpm_current_target_issue

		scope:first_party_leader.interest_group = {
			var:bpm_party_issue = {
				save_scope_as = bpm_selected_issue
			}
		}

		set_variable = {
			name = bpm_current_target_issue
			value = scope:bpm_selected_issue
		}
		
		random_active_party = {
			limit = {
				NOT = {
					any_member = {
						this = scope:first_party_leader.interest_group
					}
				}
			}
			save_scope_as = second_party
			ordered_member = {
				order_by = ig_clout
				position = 0
				leader = {
					save_scope_as = second_party_leader
				}
			}
		}
	}

	option = {
		name = bpm_issues_of_elections.1.a
		custom_tooltip = bpm_issue_relevance_down_tt
		hidden_effect = {
			save_scope_value_as = {
				name = macro_value
				value = -0.05
			}
			bpm_add_macro_issue = yes
		}
		scope:first_party = {
			add_momentum = -0.1
		}
	}

	option = {
		name = bpm_issues_of_elections.1.b
		custom_tooltip = bpm_issue_relevance_very_up_tt
		hidden_effect = {
			save_scope_value_as = {
				name = macro_value
				value = 0.2
			}
			bpm_add_macro_issue = yes
		}
		scope:second_party = {
			add_momentum = -0.3
		}
	}
}