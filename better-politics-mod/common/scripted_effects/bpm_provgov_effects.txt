bpm_prov_gov_setup_effect = {
	bpm_remove_political_stability_huge = yes
	bpm_remove_political_stability_huge = yes

	bpm_setzero_everyfront_organization_subp = yes
	
	# Laws
	save_scope_as = associated_country
	every_active_law = {
		type = {
			bpm_codification_set_traditional = yes
		}
	}

	every_in_list = {
		variable = bpm_revolution_victory_igs
		order_by = ig_clout
		position = 0
		save_scope_as = leader_ig
	}

	## Revolutionary Republic
	bpm_enact_law = { law = law_type:law_presidential_republic       level = traditional }
	bpm_enact_law = { law = law_type:law_no_separate_hog             level = traditional }
	bpm_enact_law = { law = law_type:law_advisory_body               level = traditional }
	bpm_enact_law = { law = law_type:law_autocracy                   level = decree      }
	bpm_enact_law = { law = law_type:law_appointed_bureaucrats       level = decree      }


	scope:leader_ig.leader = {
		set_character_as_ruler = yes
	}

	set_variable = bpm_marker_revolutionary_government
	recalculate_pop_ig_support = yes
}