total_months = {
    value = year
	multiply = 12
	add = month
}

bpm_party_num_legislators = {
	value = 0
	every_member = {
		limit = {
			has_variable = bpm_ig_num_legislators
		}
		add = var:bpm_ig_num_legislators
	}
}